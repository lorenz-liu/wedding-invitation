export default definePageConfig({
  /** Prevent WeChat page-level rubber-band scroll on swipe (real device). */
  disableScroll: true,
  enablePullDownRefresh: false,
  enableShareAppMessage: true,
  enableShareTimeline: true,
});
