{
  "name": "wedding-invitation-cloudflare",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute wedding-guests --remote --file=schema.sql",
    "db:migrate:local": "wrangler d1 execute wedding-guests --local --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250204.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.114.0"
  }
}
